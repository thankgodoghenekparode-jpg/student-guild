# Local development: Vite proxies /api requests to the backend.
VITE_API_PROXY_TARGET=http://localhost:5000

# Optional for deployed/static frontend builds that should call an API directly.
# VITE_API_BASE=https://your-api.example.com/api

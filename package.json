{
  "name": "student-career-guide",
  "version": "1.0.0",
  "description": "Modern mobile-first web app for secondary and university students to discover careers, explore courses, and plan study goals.",
  "main": "index.js",
  "scripts": {
    "dev": "node scripts/dev.js",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:admin": "npm run dev --prefix admin",
    "start:backend": "npm run start --prefix backend",
    "build": "npm run build --prefix frontend && npm run build --prefix admin",
    "build:frontend": "npm run build --prefix frontend",
    "build:admin": "npm run build --prefix admin",
    "setup": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix admin"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs"
}
